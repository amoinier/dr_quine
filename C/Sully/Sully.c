#include <stdio.h>
#include <string.h>
#include <stdlib.h>
int i = 5;
int main(void) { if (i > 0) { if (strchr(__FILE__, i + 48)) { i--; } char c = (i + 48); char *str = strcat(strdup(strcat(strdup("Sully_"), &c)), ".c"); char *str2 = strdup(str); str2[7] = '\0'; FILE *fp = fopen(str, "w"); if (fp) { char *quine = "#include <stdio.h>%c#include <string.h>%c#include <stdlib.h>%cint i = %d;%cint main(void) { if (i > 0) { if (strchr(__FILE__, i + 48)) { i--; } char c = (i + 48); char *str = strcat(strdup(strcat(strdup(%cSully_%c), &c)), %c.c%c); char *str2 = strdup(str); str2[7] = %c%c0%c; FILE *fp = fopen(str, %cw%c); if (fp) { char *quine = %c%s%c; fprintf(fp, quine, 10, 10, 10, i, 10, 34, 34, 34, 34, 39, 92, 39, 34, 34, 34, quine, 34, 34, 34, 34, 34, 34, 34, 10); system(strcat(strdup(strcat(strdup(strcat(strdup(%cgcc -c %c), str)), %c -o %c)), str2)); } if (i > 0) { system(strcat(strdup(%c./%c), str2)); }} return 0;}%c"; fprintf(fp, quine, 10, 10, 10, i, 10, 34, 34, 34, 34, 39, 92, 39, 34, 34, 34, quine, 34, 34, 34, 34, 34, 34, 34, 10); system(strcat(strdup(strcat(strdup(strcat(strdup("gcc -c "), str)), " -o ")), str2)); } if (i > 0) { system(strcat(strdup("./"), str2)); }} return 0;}
